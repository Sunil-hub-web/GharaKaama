<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gharakaama">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.GharaKaama">
        <activity android:name=".ThanksPage"></activity>
        <activity android:name=".AddressDetails" />
        <activity android:name=".OTPVerification" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".UserRegister" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginPage" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>